# 적대적 검증 루프 2026-06-27

확인 시각: 2026-06-27T22:49+09:00

## 결론

현재 결론은 `조건부 진행` 유지, 검증 완료 금지다. MVP와 note/Vercel 공개면은 살아 있지만, 실제 외부 응답은 0명이다.

## 재검증 루프

| 공격 질문 | 확인한 증거 | 판정 |
|---|---|---|
| 게시했다는 사실을 수요 검증으로 착각했나? | `experiments/validation_signal_summary.md` 재생성 결과 total respondents 0, online public responses 0 | 착각 금지. 게시/배포 완료일 뿐 검증 완료 아님 |
| 오프라인 placeholder가 응답으로 섞였나? | `experiments/field_interview_log.csv`는 1행이지만 `example`/`P01` placeholder이고 집계에서 제외됨 | 응답 0 판정 유지 |
| 채널 로그의 posted/public rows가 응답 수로 오염됐나? | `channel_posting_log.csv`는 8행, posted yes 4행이지만 이는 채널 상태다 | demand signal로 세면 안 됨 |
| GitHub 냄새가 소비자-facing URL에 다시 노출됐나? | Vercel `/`, `/note`, `/share?src=note_kfood`, `/field` 모두 200이고 raw HTML에서 `github.io`, `article_github`, `GitHub` 플래그 없음 | Vercel 공개면은 현재 clean |
| `/answer-note`와 note raw HTML의 `github` 플래그가 문제인가? | `/answer-note`는 Notion 내부 `githubStudentPackHome`, note는 CSS 주석 `github.com/filipelinhares/ress`에서 잡힘 | 사용자-visible CTA 문제로 보지 않음 |
| `/answer-note` 허브 전환이 live와 일치하나? | 첫 Vercel 배포는 rewrite만으로 404였으나, `docs/answer-note.html`을 직접 추가하고 재배포한 뒤 `https://korean-sesame-oil-mvp.vercel.app/answer-note?src=note_kfood`가 200, final URL 유지, title `30秒回答 | 韓国式しぼりたてごま油`로 확인됨 | live 일치. note 모바일 CTA는 이제 모바일 응답 허브로 본다 |
| GitHub waitlist 응답이 따로 생겼나? | `gh issue list --repo gurwnswh9910-maker/korean-sesame-oil-mvp` 결과 열린 이슈는 `#1 MVP validation tracker`뿐 | waitlist 응답 0 |
| note 대시보드 0을 post-publish 0반응으로 확정해도 되나? | 기존 기록의 note dashboard 집계 시각은 2026-06-27 21:45, 게시 시각은 21:46 | 아직 확정 금지. 24h heartbeat에서 다시 확인 |
| 완료 감사의 다음 작업이 최신인가? | `experiments/completion_audit.md`의 다음 실행 1번은 note GitHub 링크 교체지만, `검증/게시_배포_기록.md`와 현재 HTTP 확인상 이미 교체됨 | stale checklist. 다음 작업자는 24h 응답 확인부터 시작 |

## 실행한 확인

```powershell
$env:PYTHONUTF8='1'; $env:PYTHONIOENCODING='utf-8'; python .\scripts\summarize_validation_signals.py
$env:PYTHONUTF8='1'; $env:PYTHONIOENCODING='utf-8'; python .\scripts\test_summarize_validation_signals.py
$env:PYTHONUTF8='1'; $env:PYTHONIOENCODING='utf-8'; python .\scripts\test_record_public_social_response.py
gh issue list --repo gurwnswh9910-maker/korean-sesame-oil-mvp --json number,title,state,createdAt,updatedAt,labels --limit 20
npm exec --yes --package=vercel -- vercel deploy .\docs --prod --yes --project korean-sesame-oil-mvp
```

결과:

- `summarize_validation_signals.py`: `insufficient_external_evidence`
- `test_summarize_validation_signals.py`: pass
- `test_record_public_social_response.py`: pass
- `public_social_responses.csv`: 0행
- `field_interview_log.csv`: 1행, non-placeholder 0행
- `waitlist_responses.csv`: 0행
- GitHub issue list: 검증 트래커 `#1`만 열림
- Vercel latest inspect: `https://vercel.com/gurwnswh9910-makers-projects/korean-sesame-oil-mvp/CykJWSxZGFEiAeLfubYZUtjoSXjw`
- Production `/answer-note?src=note_kfood`: 200, no redirect, mobile390/desktop1280 overflow count 0

## 다음 작업자 시작점

1. 2026-06-28T21:50+09:00 heartbeat `note-24h`에서 note 전체뷰, 댓글, 스키, Notion 응답, 공개 검색을 다시 확인한다.
2. `/answer-note`는 모바일 응답 허브로 live 반영됐다. 다만 다음 세션 시작 시에는 production 200을 짧게 재확인한다.
3. 좋아요, 스키, 조회수, 공유는 응답 수로 세지 않는다.
4. note 외 X/Threads/Konest에는 사용자 별도 허용 전 게시하지 않는다.
5. `experiments/completion_audit.md`의 다음 실행 1번은 오래된 항목이므로, 링크 교체보다 24h 응답 확인을 우선한다.
6. 실제 응답이 생기면 원본 URL/export 경로를 남긴 뒤 `experiments/public_social_responses.csv`, `experiments/notion_submissions_export.csv`, `experiments/field_interview_log.csv` 중 맞는 파일에 기록하고 재집계한다.
